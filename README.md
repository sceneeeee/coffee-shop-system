# coffee-shop-system
CPT109 courses work( group with 5 people)
upload files to preview first(the "whole file" is generated by claude)  to folder "preview", and I split it in 5 parts, adding makefile and .h.

//default manager code is "manager123"

And the fold "complete" is the final version of code.
(the fold "preview" is kind of Chinese version without full optimization)
